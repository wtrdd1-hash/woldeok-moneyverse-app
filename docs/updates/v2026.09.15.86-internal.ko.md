# v2026.09.15.86 내부 작업 기록

## 확인 결과
- 앱 `main`은 `v2026.09.15.85` 상태였습니다.
- 앱의 `WorkFeatureViewModel`은 서버의 `daily_limit`/`taken_today`를 읽지 않았습니다.
- `PlayScreen`은 quota 소진 여부와 무관하게 근무 완료 버튼을 활성화했습니다.
- 백엔드 `v2026.09.15.102`는 작업별 실제 `daily_limit`과 서울 기준 `taken_today`를 복구하고 서버/DB에서 최종 차단합니다.

## 수정
- `WorkTaskUi`에 `dailyLimit`, `takenToday`, `remainingToday`, `quotaReached` 추가.
- camelCase/snake_case quota 응답을 모두 수용.
- 과제 카드에 오늘 사용량/한도/남은 횟수 표시.
- quota 소진 과제 완료 버튼 비활성화.
- 서버 완료 실패 후 `load()`를 호출해 최신 quota 상태 재동기화.

## 안전 경계
- 앱의 quota 차단은 UX 보조이며 최종 검증은 서버/DB가 수행합니다.
- 관리자 `featureState`는 전체 기능 운영 스위치이고 과제별 일일 quota와 별도입니다.
- HTTP 오류 body를 사용자에게 그대로 노출하지 않고 기존 상태코드 중심 정책을 유지합니다.

## 단계
- `v86-01` 계약 재확인 완료
- `v86-02` ViewModel 수정 완료
- `v86-03` Compose UI 수정 완료
- `v86-04` 서버 거부 후 상태 재조회 완료
- `v86-05` GitHub CI 및 main 병합 확인 예정
