# App v1.0.15 — Game-day visibility, dice result UI and admin logs

- Adds the authoritative Moneyverse game clock to casino/play data so users can see the current game day and next reset boundary.
- Shows the server-settled dice face with a visual die result after a roll.
- Connects the Android administrator screen to the server activity-log API.
- Keeps support-thread administration and existing economy/casino API compatibility intact.
- Works with the server-side lobby identity fix so realtime chat can display the member's actual profile nickname.
- QA: Android unit tests, lintDebug and assembleDebug completed successfully.
