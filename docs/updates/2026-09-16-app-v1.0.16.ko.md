# Woldeok Moneyverse Android v1.0.16 — QA 하드닝

- 최신 백엔드 계약 기준으로 단위 테스트, debug 빌드, Android lint를 다시 검증했습니다.
- CI/로컬에서 `./gradlew`를 직접 실행할 수 있도록 Gradle wrapper 실행 권한을 복구했습니다.
- deprecated OkHttp request-body 생성 코드를 `ByteArray.toRequestBody`로 교체했습니다.
- 커뮤니티 Send 아이콘을 RTL 대응 `Icons.AutoMirrored.Filled.Send` API로 교체했습니다.
- 이번 패치는 게임플레이/API 계약 동작을 변경하지 않습니다.
- gateway allowlist에 없는 `business-types` 레거시 구매 fallback을 제거해 사업체 구매가 정식 앱 API 경로만 사용하도록 했습니다.
