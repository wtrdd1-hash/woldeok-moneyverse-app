# Android logout and production API lock — v2026.09.14.79

## Summary

- Added a user-visible logout action to the MY screen.
- Logout calls the server logout endpoint, clears the persistent CookieJar/CSRF/local session, and immediately returns the app to the authentication screen.
- Removed the consumer-facing generic API operations console from navigation and source.
- Removed runtime API environment presets and mock switching. The Android app is now pinned to `https://easy-scraping.com/`.
- Removed the obsolete mock network interceptor.
- Bumped Android release identity to `versionCode 7`, `versionName 1.0.6`.

## QA gate

GitHub Android CI must pass `testDebugUnitTest`, `assembleDebug`, and `bundleRelease` before merge to `main`.
