# Android v1.0.14

- Aligns work and casino flows with the latest server API.
- Prevents raw HTTP 409 from being shown to users; RFC problem details are mapped to actionable Korean messages.
- Reads server work daily/weekly reward quotas before task completion and disables reward actions when the global quota is exhausted.
- Reads casino terms and refreshes them after each play.
- Keeps all mutations API-only; the app never writes to PostgreSQL directly.
