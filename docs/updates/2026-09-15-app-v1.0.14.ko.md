# Android v1.0.14

- 최신 서버 API 기준으로 근무/카지노 흐름을 다시 맞췄다.
- 사용자에게 HTTP `409` 숫자를 그대로 노출하지 않고 서버 Problem Details를 실제 원인 안내로 변환한다.
- 근무 완료 전 서버의 일/주 보상 한도를 읽고 전역 한도 소진 시 보상 버튼을 비활성화한다.
- 카지노 terms를 읽고 매 게임 뒤 상태를 다시 조회한다.
- 모든 변경은 앱 → API → 서버 코어 → DB 경로만 사용하며 앱은 DB를 직접 수정하지 않는다.
