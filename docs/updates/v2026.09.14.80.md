# v2026.09.14.80 — Android API contract alignment

## User-facing changes

- Connect all eight career choices to the server-authoritative active-job API.
- Load professional work tasks and complete eligible tasks for server-issued WLD and EXP.
- Add optional board-post image upload and attachment.
- Align the Android shop with Store 2.0 so the same catalogue shown on the web is visible in the app.
- Correct stock list, portfolio, sparkline and order response handling.
- Correct business catalogue, ownership, equity, purchase and settlement request/response handling.
- Add required idempotency keys to casino coin/dice plays and normalize casino receipts.
- Correct season event and leaderboard response wrappers.
- Normalize wallet/bank mutation handling and reload authoritative state after successful writes.
- Correct announcement and board-comment wrappers.
- Normalize service status so `operational` is displayed as healthy instead of “API/server preparing”.
- Bump Android to versionCode 8 / versionName 1.0.7.

## Contract notes

The backend remains authoritative for balances, prices, work rewards, business settlement, casino results and inventory. Android now adapts the actual `/app-api/v1/*` response envelopes instead of relying on legacy direct-array/direct-object assumptions. Session cookies, CSRF protection and idempotency requirements remain enforced by the server.

## Validation gate

The release branch must pass Android CI (`testDebugUnitTest`, `assembleDebug`, `bundleRelease`) at the final commit before merge to `main`.
