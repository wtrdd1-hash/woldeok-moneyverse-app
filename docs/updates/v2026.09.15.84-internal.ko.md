# 내부 업데이트 기록 — v2026.09.15.84

1. **v2026.09.15.84-01 — HTTP 오류 노출 정리**
   - 경제/상점/직업/커뮤니티/게시판 작성의 HTTP 실패에서 서버 error body를 사용자에게 노출하지 않도록 수정했습니다.
   - HTTP 응답 실패는 `400`, `401`, `409` 같은 상태 코드 숫자만 표시합니다.

2. **v2026.09.15.84-02 — 상점 검색**
   - 서버 `q` 검색 계약을 호출하는 앱 검색 상태와 검색 UI를 추가했습니다.

3. **v2026.09.15.84-03 — 사업 구매 목록 정리**
   - 서버가 보유 사업을 제외한 카탈로그를 제공하며 앱도 `businessTypeId` 기준으로 한 번 더 제외합니다.

4. **v2026.09.15.84-04 — 전액/전량 편의 기능**
   - 송금 현금 전액, 은행 입금 현금 전액, 출금 예금 전액, 대출 상환 가능 전액을 추가했습니다.
   - 주식은 현금/현재가 기준 최대 매수 수량과 보유 수량 기준 전량 매도를 추가했습니다.

5. **v2026.09.15.84-05 — 관리자 직업 제한 동기화**
   - `/work/tasks`의 `featureState`를 읽어 제한 중에는 앱의 전직/근무 버튼을 비활성화합니다.
   - 서버도 같은 상태를 권위 있게 검사하므로 앱 UI 우회로 실행할 수 없습니다.

6. **v2026.09.15.84-06 — 검증**
   - Android CI 성공 후 main 병합을 진행합니다.