# v2026.09.15.84 — 경제 기능 편의성과 서버 정책 동기화

## 요약

- 사용자에게 보이는 HTTP 실패는 서버 상세 응답이나 내부 문구 대신 상태 코드 숫자만 표시합니다.
- 상품명·설명·카테고리를 대상으로 API 기반 상점 검색을 추가했습니다.
- 이미 보유한 사업 유형은 서버 필터와 앱 보조 필터를 함께 적용해 구매 목록에서 제거합니다.
- 송금, 은행 입금/출금, 대출 상환, 주식 최대 매수, 보유 주식 전량 매도에 `전액/전량` 기능을 추가했습니다.
- 직업 화면은 관리자 서버의 `featureState`를 읽어 제한 상태일 때 전직·근무 동작을 비활성화합니다.

## 서버 연동

이 버전은 상점 검색, 사업 목록 권위 필터, 직업 기능 스위치 강제, HTTPS 보강을 제공하는 서버 `v2026.09.15.96`과 함께 사용합니다.

## 검증

병합 전 Android CI의 `testDebugUnitTest`, `assembleDebug`, `bundleRelease` 성공을 확인합니다.