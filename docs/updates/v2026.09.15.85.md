# v2026.09.15.85 — Gallery media and HTTP-code-only errors

## Changes
- Resolve relative gallery `imageUrl` values such as `/media/...` against the pinned production origin `https://easy-scraping.com/`.
- Upgrade accidental `http://` gallery media URLs to `https://` before Coil rendering.
- Keep gallery fetch failures user-visible as numeric HTTP status codes only.
- Make daily reward availability/claim HTTP failures show only the numeric status code.
- Includes the pending v2026.09.15.84 app changes for shop search, owned-business filtering, full-amount controls, work restrictions, and HTTP error-body suppression.

## Validation
Per operator instruction, no Android runtime/device/CI test gate was used for this promotion. Changes were reviewed by repository diff and API-contract inspection only.
