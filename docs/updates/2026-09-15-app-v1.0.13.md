# Android v1.0.13

## Changes
- Adds real-time lobby chat to the community screen.
- Adds persistent member-to-administrator support conversations.
- Adds administrator support inbox, replies, and case-status controls to the admin screen.
- Reads progression level and experience from the authoritative work/profile API instead of UI hard-coded values.
- Keeps all support traffic on the public mobile BFF (`/app-api/v1`) with session cookies and CSRF protection.

## Verification
- `testDebugUnitTest` passed.
- `lintDebug` passed.
- `assembleDebug` passed.
- `bundleRelease` passed and produced `app-release.aab`.
